{
  "$schema": "https://json-schema.org/schema",
  "$id": "GoogleClient",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the nx library to generate the client in.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the name of the client?"
    },
    "fileName": {
      "type": "string",
      "description": "The name of the client file to generate.",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What is the name of the client?"
    },
    "promptFile": {
      "type": "string",
      "description": "Additional user instructions or context for AI-assisted client generation.",
      "$default": {
        "$source": "argv",
        "index": 2
      },
      "x-prompt": "Enter the path to your prompt file. For an example see client.prompt.txt"
    }
  },
  "required": ["name"]
}
