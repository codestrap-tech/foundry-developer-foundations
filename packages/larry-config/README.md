# larry-config

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build larry-config` to build the library.

## Running unit tests

Run `nx test larry-config` to execute the unit tests via [Jest](https://jestjs.io).
